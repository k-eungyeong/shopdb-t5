# shopdb2 팀 공용 DB 설정 정리

작성일: 2026-09-22
대상: t5 (shopdb2) 팀 프로젝트

---

## 1. 문제 상황

- 팀원들이 로그인 테스트를 할 때 "아이디와 비밀번호가 올바르지 않습니다" 에러가 계속 발생.
- admin01, seller01/02, buyer01~03 계정은 DB에 `password_hash`가 `$2b$admin` 같은 가짜 값(자리채움 데이터)으로 들어있어서, 애초에 어떤 비밀번호를 넣어도 로그인이 안 되는 게 정상인 상태였음.
- buyer5는 해시 값 자체는 정상이었지만 그래도 로그인이 안 되는 경우가 있었음.
- 팀원이 자기 컴퓨터에서 비밀번호를 바꾸면 그 컴퓨터에서는 로그인이 되는데, 다른 팀원 컴퓨터에서는 여전히 안 됨.

## 2. 원인

- `t5_backend_shopdb2/.env`의 `DB_HOST=127.0.0.1`로 되어 있어서, 각 팀원이 **자기 컴퓨터 안의 로컬 MySQL**에 각자 따로 접속하는 구조였음.
- 프론트엔드(`shopApi.js`)도 `VITE_API_URL` 기본값이 `http://127.0.0.1:8000`이라, 각자 자기 컴퓨터의 백엔드만 바라봄.
- 즉 DB가 팀원 수만큼(3벌) 따로 존재하는 상태라, 한 사람이 비밀번호를 바꿔도 그건 그 사람의 로컬 DB 한 줄만 바뀌는 것이고 다른 사람의 로컬 DB에는 반영되지 않음.
- 로그인 에러 메시지 자체는 백엔드가 실제로 401 응답에 담아 보내는 진짜 에러이며, 프론트엔드(Login.jsx / AuthContext.jsx / shopApi.js) 코드 자체에는 버그가 없음을 확인함.

## 3. 해결 방법 (선택한 방법: A)

같은 네트워크(같은 와이파이/공유기)에 있을 때, 팀원 중 한 명(은경님 컴퓨터, IP: `172.30.1.47`)의 MySQL을 팀 공용 DB 서버로 임시로 열어서 사용.

> 주의: 이 방법은 은경님 컴퓨터가 켜져 있고, 팀원들이 같은 네트워크(같은 172.30.1.x 대역)에 연결돼 있을 때만 작동합니다. 장소를 옮기거나 와이파이가 바뀌면 다시 안 될 수 있습니다.

## 4. 은경(호스트) 컴퓨터에서 완료한 설정

1. **MySQL 외부 접속 허용 확인**
   - `C:\ProgramData\MySQL\MySQL Server 8.0\my.ini` 확인 결과 `bind-address` 제한 설정이 없어서, 이미 모든 IP에서의 접속을 허용하는 상태였음 (별도 수정 불필요).

2. **DB 계정에 원격 접속 권한 부여** (MySQL Workbench에서 실행)
   ```sql
   ALTER USER 'shopdbid2'@'%' IDENTIFIED BY 'shopdbid2';
   GRANT ALL PRIVILEGES ON shopdb2.* TO 'shopdbid2'@'%';
   FLUSH PRIVILEGES;
   ```
   - 완료. (`shopdbid2`@`%` 계정이 이미 존재했고, 비밀번호와 권한을 다시 확실히 맞춰줌)

3. **Windows 방화벽에 3306 포트 열기**
   - Windows Defender 방화벽 → 고급 설정 → 인바운드 규칙 → 새 규칙 → 포트 → TCP → 3306 → 연결 허용 → 이름 "MySQL"로 등록
   - 완료.

4. **은경님 본인의 `.env`는 변경하지 않음**
   - 같은 컴퓨터 안이므로 `DB_HOST=127.0.0.1` 그대로 유지해도 됨.

## 5. 팀원 두 명이 해야 할 설정

`t5_backend_shopdb2/.env` 파일에서 아래 값으로 맞추기:

```
DB_HOST=172.30.1.47      ← 변경 (은경님 컴퓨터 IP)
DB_PORT=3306              ← 그대로 유지 (포트는 안 바꿔도 됨)
DB_USER=shopdbid2         ← 본인 값이 다르면 이걸로 변경
DB_PASSWORD=shopdbid2     ← 본인 값이 다르면 이걸로 변경
DB_NAME=shopdb2           ← 그대로 유지
```

변경 후 반드시 **백엔드 서버 재시작** (`.env` 값은 서버 시작 시 한 번만 읽기 때문에 재시작해야 반영됨).

## 6. 테스트 및 확인 방법

- 백엔드 재시작 시 터미널에 DB 연결 에러가 뜨지 않는지 확인.
- 프론트엔드에서 기존 로그인 토큰이 남아있다면 로그아웃 후 다시 로그인해서 테스트 (예전 로컬 DB 기준 토큰일 수 있음).
- 안 될 경우 확인할 것:
  - 은경님 컴퓨터가 켜져 있고 같은 네트워크에 연결돼 있는지
  - 은경님 컴퓨터의 IP가 그대로 `172.30.1.47`인지 (공유기 재연결 시 IP가 바뀔 수 있음 → `ipconfig`로 재확인)
  - 팀원의 `.env`에 있는 DB_USER/PASSWORD가 `shopdbid2`/`shopdbid2`로 맞게 바뀌었는지

## 7. 참고 — 더 근본적인 해결 (추후 고려)

지금 방식은 은경님 컴퓨터가 꺼지거나 네트워크가 바뀌면 다시 안 되는 임시방편임. 이후 원격에서도 안정적으로 협업하려면 무료 클라우드 MySQL(Railway, Clever Cloud, Aiven 등)로 옮기는 방법을 고려할 수 있음. 이 경우 기존 데이터를 `mysqldump`로 한 번 내보내서 옮기고, 팀원 전체의 `.env`를 클라우드 DB 정보로 바꾸면 됨.
